unbind C-b
set -g prefix `
bind ` send-prefix
bind-key \ swap-pane -dD
set -s escape-time 0
set -g mouse on
set -g base-index 1
setw -g aggressive-resize on
set-option -g default-terminal "xterm-256color"
bind-key -n C-a send-prefix
source-file ~/.tmux/colors-blue.conf
